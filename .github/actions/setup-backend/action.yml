name: Setup Backend Environment
description: Install uv and Python dependencies for a project directory (webapp or data-platform)

inputs:
  working-directory:
    description: "Directory that contains pyproject.toml and uv.lock (e.g. webapp, data-platform)"
    required: false
    default: "webapp"
  uv-groups:
    description: 'Arguments passed to uv sync (e.g. "--group dev")'
    required: false
    default: "--group dev"

runs:
  using: composite
  steps:
    - name: Install uv
      uses: astral-sh/setup-uv@1e862dfacbd1d6d858c55d9b792c756523627244
      with:
        enable-cache: true

    - name: Cache virtual environment
      id: cache-venv
      uses: actions/cache@v4
      with:
        path: ${{ inputs.working-directory }}/.venv
        key: venv-${{ inputs.working-directory }}-${{ inputs.uv-groups }}-${{ hashFiles(format('{0}/uv.lock', inputs.working-directory)) }}

    - name: Install Python dependencies
      if: steps.cache-venv.outputs.cache-hit != 'true'
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: uv sync ${{ inputs.uv-groups }}
