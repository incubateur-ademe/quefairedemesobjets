name: 🪢 Non-régression

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize]
  push:
    branches:
      - "**"

jobs:
  baseline:
    name: Baseline (main)
    uses: ./.github/workflows/regression-job.yml
    with:
      ref: main
      update_snapshots: true
      playwright_config: next/playwright.regression.ts
      upload_screenshots: true
      download_screenshots: false
    secrets:
      SAMPLE_DB_URI: ${{ secrets.SAMPLE_DB_URI }}

  compare:
    name: Compare (current branch)
    needs: baseline
    uses: ./.github/workflows/regression-job.yml
    with:
      ref: ${{ github.ref }}
      update_snapshots: false
      playwright_config: playwright.regression.ts
      upload_screenshots: false
      download_screenshots: true
    secrets:
      SAMPLE_DB_URI: ${{ secrets.SAMPLE_DB_URI }}

  comment-visual-diffs:
    name: Comment visual regression diffs
    needs: compare
    if: |
      failure() &&
      github.event_name == 'pull_request'
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Download Playwright diffs
        uses: actions/download-artifact@v4
        with:
          name: playwright-diffs
          path: diffs

      - name: Build PR comment body
        id: body
        shell: bash
        run: |
          echo "body<<EOF" >> $GITHUB_OUTPUT
          echo "## ❌ Visual regression detected" >> $GITHUB_OUTPUT
          echo "" >> $GITHUB_OUTPUT
          echo "| Before | After | Diff |" >> $GITHUB_OUTPUT
          echo "|--------|-------|------|" >> $GITHUB_OUTPUT

          for expected in $(find diffs -name '*-expected.png'); do
            actual="${expected/-expected/-actual}"
            diff="${expected/-expected/-diff}"
            echo "| ![]($expected) | ![]($actual) | ![]($diff) |" >> $GITHUB_OUTPUT
          done

          echo "" >> $GITHUB_OUTPUT
          echo "_Generated by Playwright visual regression tests._" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Post PR comment
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/comments \
            -f body="${{ steps.body.outputs.body }}"
