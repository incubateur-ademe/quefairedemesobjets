name: 🟠 Deploy Airflow Preprod

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: read

jobs:
  airflow:
    name: 🟠 Airflow Preprod
    uses: ./.github/workflows/_deploy-airflow.yml
    secrets: inherit # pragma: allowlist secret`
    with:
      environment: preprod
      tag_name: preprod
