#!/bin/bash
set -euo pipefail

cd /app/webapp
gunicorn core.wsgi \
  --bind unix:/tmp/gunicorn.sock \
  --timeout 120 \
  --workers "${GUNICORN_WORKERS:-2}" \
  --max-requests 1000 \
  --max-requests-jitter 100 &

exec /app/bin/run
