version: 2

sources:
  - name: ae
    description: "Annuaire Entreprises (AE)"
    schema: public
    tables:
      # Views pointing to timestamped tables
      # of the same name with "_{YYYYMMDDHHMMSS}"
      # instead of "_in_use" suffix, generated by
      # Airflow DAG clone_ae_annuaire_entreprises
      - name: clone_ae_unite_legale_in_use
      - name: clone_ae_etablissement_in_use
